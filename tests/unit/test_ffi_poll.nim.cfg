# The library hands its messages out instead of calling the host back.
-d:ffiPollMode
# Low enough that the cap on uncollected replies is reached before the request queue fills.
-d:ffiMaxOutstandingRequests=64
